# Safe YouTube Viewer Configuration
# Copy this file to .env and configure for your environment
#
# SECURITY REQUIREMENTS:
#   - File permissions: chmod 600 .env (read/write owner only)
#   - File ownership: chown youtube-viewer:youtube-viewer .env (production)
#   - Never commit .env to git (already in .gitignore)

# ==============================================================================
# Database Configuration
# ==============================================================================

# Path to SQLite database file
# For local development: ./data/app.db
# For production: /opt/youtube-viewer/data/app.db
DATABASE_PATH=./data/app.db

# ==============================================================================
# YouTube API Configuration
# ==============================================================================

# YouTube Data API v3 key from Google Cloud Console
# Get your key at: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your_api_key_here

# ==============================================================================
# Server Configuration
# ==============================================================================

# Comma-separated list of allowed hostnames
# For local development: localhost,127.0.0.1
# For production: add your domain name
ALLOWED_HOSTS=localhost,127.0.0.1

# Environment mode (development or production)
# Set to 'production' on deployed server
ENVIRONMENT=development

# Debug mode (true or false)
# CRITICAL: Must be false in production for security
DEBUG=true

# ==============================================================================
# Logging Configuration
# ==============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# For development: DEBUG
# For production: INFO or WARNING
LOG_LEVEL=INFO

# Log file path
# For local development: ./logs/app.log
# For production: /opt/youtube-viewer/logs/app.log
LOG_FILE=./logs/app.log

# ==============================================================================
# Admin Password
# ==============================================================================

# IMPORTANT: Admin password is NOT stored in this file
# Set the admin password during database initialization:
#   uv run python backend/db/init_db.py <your_password>
#
# To change password later, re-run the init_db.py script
